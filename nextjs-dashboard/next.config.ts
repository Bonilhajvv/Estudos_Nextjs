import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Opções experimentais (ajuste para opções válidas, como `appDir`, `turbo`, etc.)
  experimental: {
    appDir: true, // Exemplo de configuração experimental válida
  },
  /* Outras opções de configuração */
};

export default nextConfig;